diff --git a/include/ast/segment.h b/include/ast/segment.h
index 5241868713a9328f3bfe419384f05bd6ec2ddd5d..0fa1f4bd1ea5ce5a07b3d538cfe6615a0be1886d 100644
--- a/include/ast/segment.h
+++ b/include/ast/segment.h
@@ -116,12 +116,20 @@ public:
   const auto &getSymbol() const noexcept { return FuncSymbol; }
   void setSymbol(Symbol<void> S) noexcept { FuncSymbol = std::move(S); }
 
+  uint32_t getMaxValueStackHeight() const noexcept {
+    return MaxValueStackHeight;
+  }
+  void setMaxValueStackHeight(uint32_t Height) noexcept {
+    MaxValueStackHeight = Height;
+  }
+
 private:
   /// \name Data of CodeSegment node.
   /// @{
   uint32_t SegSize = 0;
   std::vector<std::pair<uint32_t, ValType>> Locals;
   Symbol<void> FuncSymbol;
+  uint32_t MaxValueStackHeight = 0;
   /// @}
 };
 
diff --git a/include/runtime/instance/function.h b/include/runtime/instance/function.h
index 3493d4f1bb57a01c0702aee3617088d00f940e12..69e7b7862ed13ab186c5b1886fa525523a36228a 100644
--- a/include/runtime/instance/function.h
+++ b/include/runtime/instance/function.h
@@ -52,10 +52,12 @@ public:
   FunctionInstance(const ModuleInstance *Mod, const uint32_t TIdx,
                    const AST::FunctionType &Type,
                    Span<const std::pair<uint32_t, ValType>> Locs,
-                   AST::InstrView Expr) noexcept
+                   AST::InstrView Expr,
+                   uint32_t MaxValueStackHeight = 0) noexcept
       : CompositeBase(Mod, TIdx), FuncType(Type),
         CanonicalFuncTypeId(canonicalizeFuncTypeId(Type)),
-        Data(std::in_place_type_t<WasmFunction>(), Locs, Expr) {
+        Data(std::in_place_type_t<WasmFunction>(), Locs, Expr,
+             MaxValueStackHeight) {
     assuming(ModInst);
   }
   /// Constructor for compiled function.
@@ -113,6 +115,13 @@ public:
     return std::get_if<WasmFunction>(&Data)->LocalNum;
   }
 
+  uint32_t getMaxValueStackHeight() const noexcept {
+    if (const auto *WasmFunc = std::get_if<WasmFunction>(&Data)) {
+      return WasmFunc->MaxValueStackHeight;
+    }
+    return 0U;
+  }
+
   /// Getter of function body instrs.
   AST::InstrView getInstrs() const noexcept {
     if (std::holds_alternative<WasmFunction>(Data)) {
@@ -209,15 +218,17 @@ private:
   struct WasmFunction {
     const std::vector<std::pair<uint32_t, ValType>> Locals;
     const uint32_t LocalNum;
+    const uint32_t MaxValueStackHeight;
     AST::InstrVec Instrs;
     WasmFunction(Span<const std::pair<uint32_t, ValType>> Locs,
-                 AST::InstrView Expr) noexcept
+                 AST::InstrView Expr, uint32_t MaxStackHeight) noexcept
         : Locals(Locs.begin(), Locs.end()),
           LocalNum(
               std::accumulate(Locals.begin(), Locals.end(), UINT32_C(0),
                               [](uint32_t N, const auto &Pair) -> uint32_t {
                                 return N + Pair.first;
-                              })) {
+                              })),
+          MaxValueStackHeight(MaxStackHeight) {
       // FIXME: Modify the capacity to prevent from connection of 2 vectors.
       Instrs.reserve(Expr.size() + 1);
       Instrs.assign(Expr.begin(), Expr.end());
diff --git a/include/runtime/stackmgr.h b/include/runtime/stackmgr.h
index 14eec9b803fe8e2ff16774b58fbde45b231ed4f4..877a068e596475f119886abdc5235187206fd98c 100644
--- a/include/runtime/stackmgr.h
+++ b/include/runtime/stackmgr.h
@@ -84,6 +84,13 @@ public:
     ValueStack.insert(ValueStack.end(), ValVec.begin(), ValVec.end());
   }
 
+  void reserveAdditionalValues(size_t Additional) {
+    const size_t Required = ValueStack.size() + Additional;
+    if (ValueStack.capacity() < Required) {
+      ValueStack.reserve(Required);
+    }
+  }
+
   /// Unsafe pop and return the top entry.
   Value pop() {
     Value V = std::move(ValueStack.back());
diff --git a/include/validator/formchecker.h b/include/validator/formchecker.h
index b6dcde473dfed26100461079471ac270833bbcfd..3354b266807f5b60018cb986dddf28fc6615393b 100644
--- a/include/validator/formchecker.h
+++ b/include/validator/formchecker.h
@@ -62,6 +62,9 @@ public:
   auto &getTags() { return Tags; }
   uint32_t getNumImportFuncs() const { return NumImportFuncs; }
   uint32_t getNumImportGlobals() const { return NumImportGlobals; }
+  uint32_t getMaxValStackHeight() const noexcept {
+    return static_cast<uint32_t>(MaxValStackHeight);
+  }
 
   /// Helper function
   ValType VTypeToAST(const VType &V);
@@ -144,6 +147,7 @@ private:
   /// Running stack.
   std::vector<CtrlFrame> CtrlStack;
   std::vector<VType> ValStack;
+  size_t MaxValStackHeight = 0;
 };
 
 } // namespace Validator
diff --git a/lib/executor/helper.cpp b/lib/executor/helper.cpp
index 887ca0623052107238fea80e883084960f787d7f..1851b053326a737cd990c580651d382c41cb275f 100644
--- a/lib/executor/helper.cpp
+++ b/lib/executor/helper.cpp
@@ -213,6 +213,9 @@ Executor::enterFunction(Runtime::StackManager &StackMgr,
   } else {
     // Native function case: Jump to the start of the function body.
 
+    StackMgr.reserveAdditionalValues(
+        static_cast<size_t>(Func.getLocalNum()) + Func.getMaxValueStackHeight());
+
     // Push local variables into the stack.
     for (auto &Def : Func.getLocals()) {
       for (uint32_t I = 0; I < Def.first; I++) {
diff --git a/lib/executor/instantiate/function.cpp b/lib/executor/instantiate/function.cpp
index 29443c48b4bd3d88496305c1e6c94edf56c8f2f2..7d9c9b768de81388743ee95a82dbb95d3815fe0d 100644
--- a/lib/executor/instantiate/function.cpp
+++ b/lib/executor/instantiate/function.cpp
@@ -200,7 +200,8 @@ Expect<void> Executor::instantiate(Runtime::Instance::ModuleInstance &ModInst,
       ModInst.addFunc(
           TypeIdxs[I],
           (*ModInst.getType(TypeIdxs[I]))->getCompositeType().getFuncType(),
-          CodeSegs[I].getLocals(), CodeSegs[I].getExpr().getInstrs());
+          CodeSegs[I].getLocals(), CodeSegs[I].getExpr().getInstrs(),
+          CodeSegs[I].getMaxValueStackHeight());
     }
   }
   return {};
diff --git a/lib/validator/formchecker.cpp b/lib/validator/formchecker.cpp
index 05979baba2484d841141370daec72be06b701c2a..09df028c57dfbf99f36f0453daf749c53c5342cc 100644
--- a/lib/validator/formchecker.cpp
+++ b/lib/validator/formchecker.cpp
@@ -55,6 +55,7 @@ void FormChecker::reset(bool CleanGlobal) {
   CtrlStack.clear();
   Locals.clear();
   Returns.clear();
+  MaxValStackHeight = 0;
 
   if (CleanGlobal) {
     Types.clear();
@@ -2026,7 +2027,10 @@ Expect<void> FormChecker::checkInstr(const AST::Instruction &Instr) {
   }
 }
 
-void FormChecker::pushType(VType V) { ValStack.emplace_back(V); }
+void FormChecker::pushType(VType V) {
+  ValStack.emplace_back(V);
+  MaxValStackHeight = std::max(MaxValStackHeight, ValStack.size());
+}
 
 void FormChecker::pushTypes(Span<const VType> Input) {
   for (auto Val : Input) {
diff --git a/lib/validator/validator.cpp b/lib/validator/validator.cpp
index f48289d1c5901c6ab10cb6f7c26ca63676225cb9..3df3077e34fdf3fb5cf738428dfc0897ee9925bc 100644
--- a/lib/validator/validator.cpp
+++ b/lib/validator/validator.cpp
@@ -448,6 +448,10 @@ Expect<void> Validator::validate(const AST::CodeSegment &CodeSeg,
   // Validate function body expression.
   return Checker
       .validate(CodeSeg.getExpr().getInstrs(), FuncType.getReturnTypes())
+      .map([&]() {
+        const_cast<AST::CodeSegment &>(CodeSeg).setMaxValueStackHeight(
+            Checker.getMaxValStackHeight());
+      })
       .map_error([](auto E) {
         spdlog::error(ErrInfo::InfoAST(ASTNodeAttr::Expression));
         return E;
